import { data } from '../../fixtures/filled_oto_oco';
import * as OrderFactory from '../../../src/api/tdAmeritrade/orderFactory';
let orders = data
let f = OrderFactory;

//let pairs = f.extractWorkingExitPairs(orders);
//console.log(pairs[0]['STOP']);
//console.log(pairs[0]['LIMIT']);